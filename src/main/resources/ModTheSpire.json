{
  "modid": "${modID}",
  "name": "${project.name}",
  "description": "${project.description}",
  "author_list": ["${author}"],
  "version": "${project.version}",
  "mts_version": "${ModTheSpire.version}",
  "sts_version": "${SlayTheSpire.version}",
  "credits": "PvP In The Spire (what is this used for???)",
  "dependencies": ["basemod", "stslib"]
}